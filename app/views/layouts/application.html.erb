<!DOCTYPE html>
<html>
<head>

  <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Fzone</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>

</head>

<body>
  <header>
    <!-- <% if user_signed_in? %>
      <%= link_to "Salir", destroy_user_session_path, method: :delete %>
      <%= image_tag Gravatar.new(current_user.email).image_url %>
    <% end %>
    <h1>TE GUSTA O NO</h1> -->


    <nav class="navbar navbar-default custom-nav">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="#"><i class='glyphicon glyphicon-flash'>Thunder</i></a>
        </div>
        
        <ul class="nav navbar-nav navbar-right logout">
          <% if user_signed_in? %>
            <%= link_to "Salir", destroy_user_session_path, method: :delete %>
            <%= image_tag Gravatar.new(current_user.email).image_url %>
          <% end %>
        </ul>                     
      </div><!-- /.container-fluid -->
    </nav>
  </header>


  <div class='container-fluid'>
    <div class='col-md-6 col-md-offset-3 col-sm-3 text-center '>

      <h1 class='main-color'>¿TE GUSTA O NO?</h1>
        <p>Elige aquellos amig@s que te gustan de forma anonima, te avisaremos si ambos coinciden, el resto va por tu cuenta.</p>
      </div>

      <div  class='well col-md-4 col-md-offset-4 text-center col-sm-3 custom-well'>
          <% flash.each do |name, err|%>
            <%= content_tag :div, err, class: name %>
          <% end %>

          <%= yield %>
      </div>
  </div>
  <div class='container-fluid bottom'>
  <footer>
    <p>Escribenos en twitter <a href="#">@tegustaono</a></p>
  </footer>
</div>

</body>
</html>
